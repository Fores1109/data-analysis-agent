# 📖 项目导览：data-analysis-agent 说明书

> 写给作者自己看的"项目说明书"
> 建议配合 README.md（技术细节）和 PORTFOLIO.md（简历素材）一起食用。

---

## 0. 30 秒电梯陈述

> **这是一个"用自然语言问数据"的 AI 数据分析平台**：像聊天一样问"哪个月销售额最高？"，AI 会自己写 pandas 代码、执行、算出来并给你中文结论。它不是普通的问答聊天——底下有**自研的 AI Agent**（自己决定调哪些工具、写什么代码）、**代码安全沙箱**（防止 AI 生成的代码干坏事）、**记忆系统**（记得我们之前聊过什么），还有一整套正经的数据分析能力：自动调参（AutoML）、模型解释（SHAP）、时间序列预测、因果推断、A/B 实验，以及电商和游戏两个行业的完整分析场景。

---

## 1. 这个项目是什么、为什么做

- **定位**：求职作品集（数据分析师 / 数据科学 / 算法工程师 / LLM Agent 工程师方向）
- **解决什么问题**：市面上数据分析工具"要么是纯聊天（只会说不会算）、要么是固定报表（只能看预设好的图）"——这个项目把两者结合：**自然语言驱动 + 算法深度**，还覆盖电商、游戏两个垂直行业
- **内置数据**：Olist 巴西电商真实数据（99,441 订单 / 112,650 明细行 / 98,666 客户）+ 游戏模拟数据（可复现）+ 支持上传自己的 CSV/Excel/数据库

---

## 2. 来龙去脉：这个项目是怎么一步步变成现在的样子的

这是**最重要的部分**——面试官问"这个项目你做了什么"，你讲的就是这条进化线：

| 阶段 | 当时的版本 | 问题 / 动机 | 做了什么 | 结果 |
|---|---|---|---|---|
| **① 初版** | 官方库套壳 | 用 LangChain 官方 `create_pandas_dataframe_agent`（已被官方标记 experimental/废弃） | 能问答、能出图、12 个页面 | 能跑，但：**代码执行零防护、Agent 是黑盒、60MB 数据进仓库、无 CI、统计有口径错误** |
| **② 安全与工程化** | v1.1 | 上面那堆问题被指出来 | API 路径白名单（防路径穿越）、提示词只读约束、流水线 v2（解析更稳/失败重试/审计日志）、统计修正（Yates 校正、Cohen's d 加权、CV 指标统一）、数据移出仓库、GitHub Actions CI、依赖锁定 | 从"能跑"变成"**安全可用、可复现**" |
| **③ CI 修复** | v1.1.1 | CI 第一次跑挂了（3 个隐藏问题） | Python 3.11 f-string 语法兼容、补 fastapi 依赖、Windows 盘符路径跨平台统一拒绝 | **CI 全绿**，暴露了"本地能跑 ≠ 别人能跑" |
| **④ 深度重构** | v2 | Agent 还是"套壳"、沙箱还是"软约束"、统计还是"教学级" | **自研 ReAct Agent**（LangGraph 手写显式循环 + 自定义工具集 + 多轮记忆）、**硬沙箱**（AST 静态检查→受限环境→独立子进程→超时熔断）、OLS/DID 升级 statsmodels（HC1 稳健标准误）、流失预警 v2（窗口特征 + GBDT + 时间切分 + 最优阈值，**AUC 0.54→0.65**） | 从"作品集"变成"**能撑住 LLM Agent 岗追问**" |
| **⑤ 分层记忆** | v2.1 | 记忆只是"截断"，长对话丢上下文 | **三层记忆**：工作记忆（最近 N 轮）+ 滚动摘要（LLM 压缩早期对话）+ 长期记忆（结论文档 TF-IDF 向量检索） | 第 5 轮还能引用第 1 轮的结论——**记忆管理考点达标** |

> 💡 **一句话讲进化**："一开始我套了官方库能跑就行 → 被指出安全问题 → 我自己做了硬沙箱和路径白名单 → 干脆把 Agent 也自己写了 → 最后补上记忆系统。每一步都是'发现问题→解决问题'，这就是面试官想听的。"

---

## 3. 整体架构（一张图看懂）

```
你（用户）
  │  在网页上打字提问："哪个月销售额最高？"
  ▼
Streamlit 网页（12 个页面）
  │
  ├── 数据源层    上传 CSV/Excel · 数据库(SQLAlchemy) · API 接口 · 内置示例
  │               （Olist 电商 9.9万订单 / 游戏模拟数据）
  ├── Agent 层    ★ 自研 ReAct Agent（本项目的核心创新）
  │               ├─ 工具集：6 个工具（看形状/看列/看前几行/看统计/看分布 + 写代码执行）
  │               ├─ 沙箱：AI 生成的代码先安检再执行（4 道防线）
  │               └─ 记忆：工作记忆 + 滚动摘要 + 长期检索（3 层）
  ├── 算法层      AutoML(Optuna) · SHAP 解释 · SARIMA 预测 · 因果推断(DID)
  │               · A/B 实验 · 流失预警 · RFM 分层 · 留存/付费分析
  ├── 服务层      FastAPI（可 Docker 部署成 API 服务）
  └── 质量层      GitHub Actions CI（8 套测试，3.11/3.12 全绿）
```

---

## 4. 文件地图：每个文件夹/文件是干嘛的

```
data-analysis-agent/
├── web/                    ← 用户看到的网页（Streamlit）
│   ├── app.py              ← 首页：加载数据 + 数据概览
│   └── pages/              ← 12 个功能页面（数字前缀 = 侧边栏顺序）
│       1_自然语言分析.py     ← ★ 核心页面：问 AI 问题（单 Agent / 多 Agent 流水线）
│       2_图表可视化.py       ← 7 种交互图
│       3_SQL助手.py         ← 自然语言生成 SQL
│       4_A_B实验.py          ← t 检验 / 转化率 z 检验（Yates 校正）
│       5_机器学习.py         ← Optuna 自动调参
│       6_因果推断.py         ← OLS / DID（statsmodels）
│       7_报告生成.py         ← 导出 Markdown/HTML 报告
│       8_模型解释.py         ← SHAP
│       9_销售预测.py         ← SARIMA 预测 + 异常检测
│       10_RFM分层.py         ← 客户价值分层
│       11_游戏数据分析.py     ← 留存/活跃/付费
│       12_游戏深度分析.py     ← ★ 演示首选：流失预警（亮点）
│
├── app/                    ← 核心逻辑（算法 + Agent，不依赖网页）
│   ├── agent.py            ← ★ 自研 ReAct Agent（LangGraph 显式循环 + 工具 + 沙箱）
│   ├── memory.py           ← ★ 分层记忆（工作/摘要/长期检索）
│   ├── pipeline.py         ← 多 Agent 流水线（规划→分析→报告）
│   ├── llm.py              ← LLM 工厂（DeepSeek/OpenAI/Ollama 一键切换）
│   ├── config.py           ← 读 .env 配置
│   ├── data_source.py      ← 加载 CSV/Excel/JSON/数据库/API
│   ├── charts.py           ← Plotly 图表封装
│   ├── automl.py           ← Optuna 自动调参
│   ├── ml_runner.py        ← 自动选模型训练
│   ├── explain.py          ← SHAP 解释
│   ├── forecast.py         ← SARIMA/STL/异常检测
│   ├── experiments.py      ← A/B 检验 + OLS/DID（statsmodels）
│   ├── rfm.py              ← RFM 客户分层
│   ├── sql_assistant.py    ← SQL 助手
│   ├── report.py           ← 报告生成
│   ├── game_analytics.py   ← 游戏留存/活跃/付费指标
│   ├── game_deep.py        ← 关卡漏斗/首充/LTV
│   ├── game_churn.py       ← ★ 流失预警（窗口特征 + 3 模型 + 时间切分）
│   ├── olist_loader.py     ← Olist 数据加载
│   └── theme.py            ← 网页主题
│
├── api/main.py             ← FastAPI 服务（含 data_path 路径白名单安全校验）
├── tests/                  ← 8 套自动化测试（CI 跑，不耗 LLM token）
│   ├── test_smoke.py          数据/图表/A-B/OLS/DID/机器学习
│   ├── verify_fixes.py        路径白名单/规划解析/Yates/Cohen's d
│   ├── test_sandbox.py        ★ 沙箱安全：13 个攻击面用例
│   ├── test_memory.py         ★ 分层记忆：17 个用例（FakeLLM）
│   ├── test_algo.py           AutoML/SHAP/时序/RFM
│   ├── test_game.py           游戏留存/活跃/付费
│   ├── test_game_advanced.py  关卡/首充/LTV/流失预警
│   ├── test_web.py            12 个页面渲染
│   ├── test_agent_e2e.py      ★ 真实 LLM 端到端（需 API key，CI 不跑）
│   └── test_agent_memory_e2e.py ★ 记忆端到端（需 API key，CI 不跑）
│
├── scripts/
│   ├── download_data.py     ← Olist 数据一键下载（数据不入库）
│   └── run_local.ps1 / run_api.ps1 / push_github.ps1
├── .github/workflows/ci.yml ← CI：Python 3.11/3.12 × 8 套测试
├── 启动数据分析Agent.bat     ← Windows 双击启动
├── requirements*.txt        ← 依赖（含精确锁定 requirements.lock.txt）
└── data/                    ← 本地数据（olist/ 已 gitignore）
```

---

## 5. 核心创新点（相对普通作品集，这就是差异化）

### 5.1 ★ 自研 Agent（不是套壳）

- **普通作品**：调用官方 `create_pandas_dataframe_agent`，面试官一句"这不是官方废弃的吗"就打穿了
- **你的做法**：用 LangGraph **手写**了 Agent 循环——`agent 节点（LLM 想）→ 工具节点（执行）→ 条件边（决定继续还是结束）`，循环上限 8 轮，全程消息可审计
- **工具协议**：6 个工具（看形状/列/前几行/统计/分布 + 通用代码执行），用 LLM 原生 function calling（工具 = 类型注解 + 中文描述，LLM 返回结构化调用）
- **能讲**：图结构、工具协议设计、防死循环、状态管理

### 5.2 ★ 硬沙箱（AI 生成代码的安全执行）

- **问题**：AI 会生成 Python 代码并在你机器上跑——"删文件、访问网络"怎么办？
- **你的做法**（四道防线，全部代码级拦截，不是提示词求它别干）：
  1. **AST 静态检查**：执行前扫描代码，禁止 `os/subprocess/open/eval` 和写文件属性（`to_csv` 等）
  2. **受限执行环境**：只给白名单模块（pandas/numpy/math...）+ 过滤后的安全 builtins
  3. **独立子进程**：代码在单独进程跑，崩了不影响主程序
  4. **超时熔断**：30 秒强制终止
- **证据**：`tests/test_sandbox.py` 13 个用例（import os / open() / to_csv / eval / socket / 死循环 全部拦截）

### 5.3 ★ 分层记忆（记得我们聊过什么）

- **工作记忆**：最近 6 轮对话原文
- **滚动摘要**：超过 8 轮后，用 LLM 把早期对话压缩成要点（旧摘要+新溢出→新摘要），防上下文爆炸
- **长期记忆**：每轮 (问,答) 结论文档入库，用**字符级 n-gram TF-IDF 向量 + cosine** 本地检索（零外部依赖、不花钱、不联网），每轮把最相关的历史结论注入提示词
- **实测效果**：第 5 轮问"我之前问的最高月份是多少？"，AI 正确答出第 1 轮的 **124,048 元**

### 5.4 统计严谨性（每个细节都能讲）

- AutoML：CV 优化目标与最终排序指标**统一**（分类 F1(weighted)，避免"用 accuracy 调参却按 F1 排名"）
- A/B 转化率检验：默认 **Yates 连续性校正**（更保守）
- Cohen's d：**自由度加权**合并标准差（不是简单平均）
- OLS/DID：**statsmodels**（HC1 异方差稳健标准误 + 95% 置信区间）
- 流失预警：**时间切分**（按最近活跃日排序，用过去预测未来，防信息泄漏）+ **Youden's J 最佳阈值**

### 5.5 工程化（作品集里少见）

- **8 套测试** + GitHub Actions CI（Python 3.11/3.12 全绿）
- 依赖锁定（requirements.lock.txt）
- **60MB 数据不进仓库**（.gitignore + 一键下载脚本），clone 秒级完成
- 数字口径真实（订单 99,441 不是"11 万"——实测后修正，README/简历/代码三处一致）

---

## 6. 更新情况（GitHub 上的提交历史，5 次大提交）

```bash
d4085f0 分层记忆系统：工作记忆+滚动摘要+长期TF-IDF检索   ← 最新（第 5 次）
c998381 深度重构：自研ReAct Agent+硬沙箱/statsmodels因果推断/流失预警v2  ← 第 4 次（最大改动）
487400d 加固 API 路径白名单：显式拒绝 Windows 盘符路径     ← CI 修复 3
7764313 修复 CI 失败：f-string 兼容 3.11 + fastapi 依赖    ← CI 修复 2
15781ea 安全加固与工程化升级：流水线v2/统计修正/CI/数据出库  ← 第 2 次
（更早）初始版本：官方套壳 + 60MB 数据入库 + 无 CI
```

**每次提交都在 GitHub 上可见**——面试官点开 commit 历史就能看到"从套壳到自研"的完整进化，这本身是加分项。

---

## 7. 怎么跑起来 + 5 分钟演示路线

```powershell
# 本地跑（Windows）
双击「启动数据分析Agent.bat」     # 自动装依赖 + 启动
# 或手动：
pip install -r requirements.txt
cp .env.example .env             # 填入 DEEPSEEK_API_KEY
streamlit run web/app.py         # 打开 http://localhost:8501
```

**演示路线（1 分钟出结果，面试/录屏用）**：
1. 首页加载"示例销售数据"
2. 进「1_自然语言分析」问："哪个月销售额最高？"——展示 AI 自动分析
3. 进「12_游戏深度分析」→ 生成模拟数据 → 点「🚨 训练流失预警模型」——展示留存/流失高危名单（最好看的画面）
4. 想秀安全：让 AI "删除一个文件"，看它礼貌拒绝

---

## 8. 面试怎么讲（10 分钟版）

1. **开场（30 秒）**：自然语言驱动 + 算法深度的数据分析平台，电商+游戏两个场景
2. **技术点 1（自研 Agent）**：为什么不用官方库 → 手写 LangGraph 循环 → 工具协议 → 记忆
3. **技术点 2（硬沙箱）**：AI 代码安全四道防线 → 13 个攻击面测试
4. **技术点 3（记忆）**：三层记忆 → 实测跨轮引用数字结论
5. **业务故事（流失预警）**：87% 类别不平衡 → class_weight → 时间切分防泄漏 → AUC 0.54→0.65 → 预警→召回→再评估闭环
6. **统计严谨性**：Yates / HC1 / 口径统一——"我发现了口径不一致并主动修正"
7. **工程化**：CI 全绿、8 套测试、数据出库、依赖锁定

---

## 9. 已知边界（诚实清单，被问到要能说清楚）

| 边界 | 现状 | 应对说法 |
|---|---|---|
| 沙箱对抗性 | 静态分析可被精心构造的代码绕过 | "常规攻击面全覆盖；对抗性场景业界通用做法是叠加容器隔离（gVisor/Firecracker）" |
| Agent 智能上限 | 取决于底层 LLM（默认 DeepSeek） | "框架是模型无关的，换 GPT/Claude 工具协议不变" |
| 记忆检索 | TF-IDF（无语义泛化） | "检索接口已抽象，可无缝换 embedding；TF-IDF 是零依赖可复现的选择" |
| DID | 单期教学级（无聚类标准误/平行趋势检验） | "statsmodels 实现，正式研究可扩展面板/事件研究" |
| API 认证 | 无内置认证 | "部署时前置鉴权 + 勿直接暴露公网（README 已声明）" |
| 测试 | 脚本式（非 pytest） | "CI 能跑；迁 pytest 是已知的下一步" |

---

## 10. 名词速查表（大白话）

| 术语 | 大白话 |
|---|---|
| **Agent** | 一个会"自己决定下一步干嘛"的 AI 程序：想→调用工具→看结果→再想，直到给出答案 |
| **ReAct** | Reasoning + Acting：AI 边推理边行动（想一下→做一步→看结果）的循环模式 |
| **LangGraph** | LangChain 官方的"图编排"库：把 AI 的工作流程画成节点和边的图 |
| **工具（tool）** | 给 AI 用的"技能"：比如"看数据前 5 行""执行一段 pandas 代码" |
| **function calling** | LLM 的"选技能"能力：你给它工具清单，它返回"我要用第 2 个，参数是…" |
| **沙箱** | 让代码在一个"笼子"里跑：能算数，但碰不到文件系统/网络/系统命令 |
| **AST** | Python 代码的"语法树"：把代码拆成结构，执行前检查每个节点安不安全 |
| **TF-IDF** | 一种给文本打分的算法：出现得多且罕见的词权重高；字符 n-gram 版对中文有效 |
| **cosine 相似度** | 衡量两个向量（文本）方向有多接近：1=几乎一样，0=不相关 |
| **滚动摘要** | 对话太长时，让 AI 把旧对话压缩成摘要，再接新对话继续压缩（像滚雪球） |
| **Optuna/TPE** | 自动调参工具：帮模型试超参数组合，TPE 是它的聪明采样算法 |
| **SHAP** | 解释"模型为什么这么预测"：算出每个特征对预测的贡献 |
| **SARIMA** | 带季节性的时间序列预测模型（比如电商的"周末波动"） |
| **DID（双重差分）** | 评估"干预效果"的方法：实验组前后变化 − 对照组前后变化 |
| **AUC** | 模型区分好坏的能力：0.5=瞎猜，0.7+=不错，1=完美 |
| **Yates 校正** | 小样本时让统计检验更保守的小修正 |
| **RFM** | 按"最近购买/频率/金额"给客户分价值等级 |
| **LTV** | 一个用户一辈子能带来的收入（本项目口径=累计收入÷注册用户数） |
| **Cohort（同期群）** | 同一天/同一周注册的一批用户，用来算留存 |
| **时间切分** | 按时间先后切训练/验证集，模拟真实"用过去预测未来"，防止偷看未来 |
